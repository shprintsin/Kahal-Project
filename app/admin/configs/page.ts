export { createPageFormConfig, createPageEditorLayout } from "./page-config";

// Placeholder page component to satisfy Next.js route expectations; actual config
// helpers are re-exported above.
export default function AdminConfigsPagePlaceholder() {
  return null;
}
